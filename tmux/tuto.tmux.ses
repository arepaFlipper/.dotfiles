#!/bin/sh
# -t: tag
# -n: name
# -d: dettach
session1="tuto_logs"

# folder_name="Josh_tried_coding/breadit"
folder_name="js_mastery/pricewise"
branch="main"
window11="lazygit"
pane111=${window00}.1
window12="lazygit"
window13="neovim"
session2="tuto_repo"
window21="lazygit"
session3="tuto_code"
window31="neovim"
path="$HOME/Documents/yt-tutos/${folder_name}/"
# tmux kill-session -t $session0;
cd ${path}
# tmux new -s $session0;

# session 1
tmux new -s $session1 -n $window11 -d
tmux send-keys -t $window11 'lazygit' C-m

# session 2
tmux new -s $session2 -n $window21 -d
tmux send-keys -t $window21
#
tmux new-window -n $window13
tmux send-keys -t $window13 'lvim .' C-m

tmux new -s $session3 -n $window31 -d
tmux send-keys -t $window31
#
tmux attach -t $session2:1
